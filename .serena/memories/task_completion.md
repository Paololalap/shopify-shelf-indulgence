# Task Completion

## When Done
1. Run `npm run format` to format Liquid files
2. Check for syntax errors in Liquid templates
3. Verify image assets exist in `assets/` directory
4. Test responsive design on mobile viewports

## Validation
- Ensure all `{% schema %}` blocks are valid JSON
- Check that image references match actual asset filenames
- Verify CSS classes are consistent across sections